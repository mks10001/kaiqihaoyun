已为您整理好项目的完整需求规范文档，并格式化为标准 Markdown。您可以直接将其复制保存为 **`PRD.md`** 文件，作为后续敏捷迭代、测试验收和扩展肉身服务器/后端接口的开发依据。

---

# 产品需求文档 (PRD.md)
**项目名称**：“开启你的好运”单页面抽奖营销系统  
**文档版本**：v1.0 (MVP 稳定版)  
**更新时间**：2023年/当前日期  
**文档状态**：已确认 / 实施基线  

---

## 1. 产品目标和使用场景

### 1.1 产品目标
* **转化与营销**：通过“1-13数字抽奖”机制（偶数免费送、奇数低价购），提升用户互动兴趣与商品转化率。
* **数据收集**：高效率收集潜在客户的姓名、联系电话与收货地址。
* **私域引流与付款控制**：通过限制奇数中奖用户的再次抽奖行为，引导其扫描微信二维码添加客服或扫描付款码完成支付，再由管理员在后台确认后解除锁定。
* **低成本开箱即用**：采用单文件 HTML/JS 架构，无需后端数据库即可通过浏览器 `localStorage` 完成投屏展示、演示或轻量营销活动。

### 1.2 使用场景
* **线上营销活动**：商家放置于公众号、社交平台引流的单页活动。
* **线下展会/投屏展示**：展位现场引流，观众现场扫码或按键抽奖。
* **私域裂变测试**：低成本验证特定商品的奇偶概率及转化率。

---

## 2. 整体页面框架和导航结构

本系统采用**单页面自适应（Responsive SPA）**结构，前端由 Tailwind CSS 渲染，主要分为两大界面区块：

```
+------------------------------------------------------------------------+
|                            [页头 Header]                               |
|        品牌 Title / 活动规则声明 / 后台管理入口 (隐藏密码弹窗)          |
+------------------------------------------------------------------------+
|                                                                        |
|  [主内容区 Main Section - 响应式双栏 Layout]                           |
|  +-----------------------------------+------------------------------+  |
|  | 左侧/中央：主展区                  | 右侧：实时中奖播报           |  |
|  | - 活动商品 Card (图片/标题/描述)  | - 滚动展示最近中奖记录       |  |
|  | - Canvas 幸运大转盘                | - 包含模拟数据与真实本地提交 |  |
|  | - 抽奖控制按钮 (开始抽奖/分享)     |                              |  |
|  +-----------------------------------+------------------------------+  |
|                                                                        |
|  [下方扩展区 Extended Section]                                         |
|  - 用户建议与留言上架区                                                |
+------------------------------------------------------------------------+
|                            [页脚 Footer]                               |
|            发货动态跑马灯跑道 / 版权及免责声明 ("不支持退货")           |
+------------------------------------------------------------------------+
|                         [全局弹窗 Modals]                              |
| - 收货信息填写弹窗 (姓名/电话/地址)                                    |
| - 结果展示与支付锁定弹窗 (微信二维码/付款二维码)                      |
| - 后台管理大弹窗 (配置/订单/发货/留言 4 大 Tab)                        |
| - 分享链接复制弹窗                                                     |
+------------------------------------------------------------------------+
```

---

## 3. 首页的内容结构

1. **顶部导航栏 (Header)**：包含活动主标题“开启你的好运”、活动规则简述及隐蔽的“管理后台”入口图标。
2. **主展示区 (Hero Area)**：
   * **商品卡片**：展示当前抽奖的限定商品图片、商品标题、详细描述及“不支持退货”的醒目标签。
   * **Canvas 抽奖大转盘**：400x400 高清大转盘，标注 1-13 数字（奇数红底、偶数绿底），中央放置黑色指针。
   * **互动按钮组**：【点击抽奖】主按钮与【分享给好友】次要按钮。
3. **右侧 / 侧边栏 (Sidebar)**：
   * **实时中奖播报**：动态垂直滚动的榜单，实时合成真实订单与保底模拟播报。
4. **下方互动区 (Community Area)**：
   * **商品建议留言板**：支持用户提交想上架的商品名称及建议。
5. **底部固定跑马灯 (Bottom Banner)**：
   * **最新发货动态**：横向无缝滚动已派发/已发货的订单物流播报。

---

## 4. 每个模块的用途

| 模块名称 | 主要用途 |
| :--- | :--- |
| **商品展示模块** | 呈现活动核心吸引物，增强用户参与抽奖的意愿。 |
| **Canvas 大转盘模块** | 提供核心视觉动画交互，通过 1.5 秒快速旋转增加抽奖的紧张感与真实感。 |
| **收货信息弹窗** | 在抽中数字后强制拦截，收集用户的真实姓名、手机号及详细地址。 |
| **中奖结果与支付锁弹窗** | 偶数显示“免费免单+微信二维码”；奇数显示“付费购买+付款码+客服微信”，并提示锁定状态。 |
| **中奖播报模块** | 营造万人参与的热烈气氛，增强社会认同感（Social Proof）。 |
| **底部发货跑马灯** | 展现真实的履约发货状态，建立信任度。 |
| **商品留言模块** | 收集用户反馈，作为后续商品选品的依据。 |
| **后台管理系统 (Admin Modal)** | 供网站主配置参数、设置概率、查看订单、标记确认付款解锁、标记发货及管理留言。 |

---

## 5. 每个模块第一版最必要的数据和操作

### 5.1 商品与转盘模块
* **数据**：商品标题、商品描述、商品图片 URL / Base64、转盘数组 `[1,12,3,10,5,8,7,6,9,4,11,2,13]`。
* **操作**：点击【开始抽奖】（触发概率计算与 Canvas 旋转）。

### 5.2 抽奖锁定与结果模块
* **数据**：当前用户标识 (`userIp`)、锁定状态 (`isLocked`)、抽中数字 (`drawnNumber`)。
* **操作**：
  * 若被锁定，点击抽奖直接弹出【付款/加微信弹窗】。
  * 未锁定，抽中后弹窗填写收货信息：`姓名 (name)`、`手机号 (phone)`、`详细地址 (address)`。
  * 提交收货信息后保存订单，展示对应的二维码。

### 5.3 后台管理模块 (管理员密码默认：`ad123ad`)
* **数据**：
  * **配置**：商品标题、描述、商品图、微信二维码、付款二维码、奇数概率（支持 0.0 ~ 1.0）。
  * **订单列表**：用户标识、姓名、电话、地址、抽取数字、奇偶类型、创建时间、状态。
  * **发货列表**：已确认/免单订单的发货状态控制。
  * **留言列表**：提交时间、IP、留言内容。
* **操作**：
  * **确认付款并解锁**：将奇数订单状态更新为`付款已确认 (已派单)`，并解除对应用户的 `isLocked` 状态。
  * **发货标记**：点击【标记发货】更新订单状态为`已发货`。
  * **图片上传**：支持从本地选择图片自动转换为 Base64 存储。
  * **数据清理**：清空订单、删除单条留言、清空留言。

---

## 6. 首页和各模块之间的关系

```
                           +-------------------+
                           |   首页 (Index)    |
                           +---------+---------+
                                     |
         +---------------------------+---------------------------+
         |                           |                           |
         v                           v                           v
+------------------+       +------------------+       +------------------+
| 商品与转盘交互   |       | 状态广播与留言   |       | 管理后台 (Modal) |
+--------+---------+       +--------+---------+       +--------+---------+
         |                           |                           |
         v                           v                           v
(触发算法计算奇偶)         (显示实时播报/底部跑马灯)   (配置修改/概率调整)
         |                                                       |
         v                                                       v
[拦截检查: 是否锁定?]                                   (更新 localStorage)
   |           |                                                 |
(未锁定)    (已锁定)                                            |
   |           |                                                 |
   v           +---------------> [直接展示付款二维码]             |
[填写收货表单]                         ^                         |
   |                                   |                         |
   v                                   |                         |
[保存订单并锁定]-----------------------+                         |
   ^                                                             |
   |                                                             |
   +--------------------[管理员点击确认付款解除锁定]-------------+
```

---

## 7. 本地数据文件、备份和恢复要求

为了保证单文件 HTML 在没有服务器后端的情况下独立运行，系统使用 HTML5 `localStorage` 机制：

### 7.1 存储键名规范 (v2 规范)
* **系统配置**：`lottery_config_v2`
* **订单列表**：`lottery_orders_v2`
* **留言列表**：`lottery_msgs_v2`
* **锁定状态**：`lottery_locks_v2`

### 7.2 数据备份与恢复要求
1. **自动回退默认值**：若 `localStorage` 无数据或解析报错，系统必须自动加载默认配置（默认奇数概率 70%）。
2. **防注入与安全处理**：所有存储并渲染到 DOM 的文本（如姓名、地址、留言、商品标题）必须经过 HTML 实体转义 (`escapeHtml`) 过滤，防止 XSS 攻击。
3. **数据清空能力**：后台提供【清空所有订单】与【清空所有留言】功能，操作前需弹窗二次确认。

---

## 8. 第一版必须完成的功能 (MVP In-Scope)

- [x] **单文件纯前端架构**：所有 HTML、CSS (Tailwind CDN)、JavaScript 整合于单个 `index.html`。
- [x] **1-13 数字抽奖转盘**：基于 HTML5 Canvas 绘制，黑色指针居中，快速旋转动画（1.5 秒 / 8 圈）。
- [x] **奇偶数概率可调**：后台支持 50%、60%、70%、80%、90% 及 100% 奇数（必中付费）、100% 偶数（必中免单）测试模式。
- [x] **收货信息强制收集**：无论奇数偶数，中奖后均需先填写姓名、手机号、详细地址。
- [x] **奇数支付锁定机制**：抽中奇数后用户进入锁定状态，在管理员于后台“确认付款”前，再次点击抽奖直接弹出付款界面，阻止重复抽奖。
- [x] **双二维码展示**：
  * 偶数中奖：展示“微信二维码”（领取免单/联系客服）。
  * 奇数中奖：同时展示“付款二维码”与“微信二维码”。
- [x] **前端管理后台**：隐藏入口、暗号/密码校验（默认 `ad123ad`），含【系统配置】、【订单管理】、【发货管理】、【留言管理】四大 Tab。
- [x] **本地图片上传**：支持在后台选取本地文件并直接转为 Base64 DataURL 保存使用。
- [x] **双播报系统**：右侧中奖垂直滚动播报 + 底部发货横向跑马灯。
- [x] **用户建议留言板**：前台提交、后台查看与删除。
- [x] **分享与公网 IP 兼容**：点击分享复制当前 URL，内置公网 IP 获取及获取失败时的本地 Simulated IP 降级方案。

---

## 9. 第一版暂时不做的功能 (MVP Out-of-Scope)

1. **真实后端数据库与跨设备同步**：不包含 MySQL/MongoDB 或 Node.js/Python 服务端，不同设备之间的订单数据不互通。
2. **真实在线支付回调**：不支持微信支付/支付宝官方 API 实时回调自动解锁，必须由人工核对后在后台点击【确认付款】。
3. **物流 API 对接与单号录入**：仅提供“未发货/已发货”状态切换，不进行真实快递单号查询与对接。
4. **原生 APP / 社交平台 SDK 分享**：不集成微信 JSSDK、QQ 分享等原生 API，统一采用剪贴板链接复制。
5. **旧版 localStorage 数据自动迁移**：v1 版本的历史数据不进行自动向下兼容迁移。

---

## 10. 可以实际检查的验收标准

| 序号 | 测试项 | 操作步骤 | 预期结果 |
| :--- | :--- | :--- | :--- |
| **1** | **转盘绘制与旋转** | 点击【点击抽奖】 | 旋转动画流畅（约 1.5s），最后准确停在目标数字扇区，黑色指针指向正确。 |
| **2** | **表单收集与弹窗** | 首次抽奖停下后 | 弹出收货信息表单，不填写直接提交提示错误；填写完整提交后进入结果界面。 |
| **3** | **偶数免单流程** | 调整概率为0%(全偶数)抽奖 | 结果弹窗显示“恭喜免单”，仅展示微信二维码；订单状态为`已派发(免单)`。 |
| **4** | **奇数购买与锁定** | 调整概率为100%(全奇数)抽奖 | 结果弹窗显示“特价购买”，同时展示付款码与微信码；**再次点击抽奖直接弹出付款码**，不旋转转盘。 |
| **5** | **后台确认解锁** | 打开后台->订单管理->点击【确认付款】 | 该订单状态变为`付款已确认 (已派单)`；前台再次点击【点击抽奖】**解锁成功**，可重新旋转抽奖。 |
| **6** | **后台图片上传** | 在后台选择本地图片上传 | 预览图立即更新，保存后前台商品图/二维码生效。 |
| **7** | **留言功能** | 前台提交留言 | 前台列表实时显示；后台【留言管理】出现该条留言，点击删除后消失。 |
| **8** | **发货状态管理** | 在后台发货管理点击【标记发货】 | 状态更新为`已发货`，底部发货跑马灯同步更新展示。 |
